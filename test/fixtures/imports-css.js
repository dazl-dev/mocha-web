import "./style.css";

describe("suite", () => {
  it("should pass", () => {
    // do nothing
  });
});

console.log(`body background-color: ${window.getComputedStyle(document.body).backgroundColor}`);
